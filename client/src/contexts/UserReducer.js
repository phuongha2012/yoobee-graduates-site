const UserReducer = (state, action) => {
    // switch (action.type) {
    //     case 'login': {
    //         const { username, password } = action.payload;
    //         return {
    //             ...state,
    //             hasLoginError: false,
    //             user
    //         }
    //     }
    // }
}

export default UserReducer;